"use client";

import { createTheme } from "@mui/material/styles";

const theme = createTheme({
	palette: {
		neutral: {
			main: "#6e6e6e",
			light: "#a8a8a8",
			dark: "#4d4d4d",
			contrastText: "#fff",
		},
	},
});

export default theme;
